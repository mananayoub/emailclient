import { AuthHttpIncterceptor } from './auth-http-incterceptor';

describe('AuthHttpIncterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthHttpIncterceptor()).toBeTruthy();
  });
});
